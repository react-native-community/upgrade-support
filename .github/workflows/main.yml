name: Autocloser
on:
  issues:
    types: [edited]
jobs:
  autoclose:
    runs-on: ubuntu-latest
    steps:
    - name: Autoclose issues that do not follow the issue template
      run: |
        echo "Hello World"
